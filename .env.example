# Server
PORT=3002
NODE_ENV=development
JWT_SECRET=replace-with-a-secure-random-string
JWT_EXPIRES_IN=7d

# OpenAI (for AI features in API routes)
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4-turbo

# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Frontend
# Used by Vite client to talk to the API
VITE_API_URL=http://localhost:3002

# Notes
# 1) Copy this file to `.env` for local development.
# 2) Never commit `.env` (it is gitignored).
# 3) In production, configure these in your hosting provider (e.g., Vercel).