{
  "name": "api-cloudflare",
  "version": "0.1.0",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "npx wrangler deploy",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@supabase/supabase-js": "^2.0.0",
    "wrangler": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
