{
  "name": "api-cloudflare",
  "version": "0.1.0",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --config wrangler.toml",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --config wrangler.toml"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "@supabase/supabase-js": "^2.45.0",
    "wrangler": "^3.78.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@cloudflare/workers-types": "^4.20240925.0"
  }
}
